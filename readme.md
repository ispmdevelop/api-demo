# Api Demo

## Swagger API documentation
This project has type metadata that generates a Swagger Open Api specification under the url: `http://${api_host}/docs`

## Run this project locally
1. Install project dependencies `npm run install`
2. Setup environment variables using .env file guide yourself with .env.example 
3. run dev command  `npm run dev`